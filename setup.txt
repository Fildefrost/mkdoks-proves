Entorno Python:

python -m venv venv
venv\Scripts\activate 
pip install -r requirements.txt

Guardar
pip freeze > requirements.txt

Proceso para subir writeups;

- Exportar writeups de notion por plataforma (todo)
- Copiar los archivos dentro de la carpeta : \docs\notion_export
- Ejecutar el script : Python .\notion_export.py "Plataforma"
- Copiar el contenido del nav_generated.yml que se ha creado en el \mkdocs.yml 